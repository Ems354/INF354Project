@page
@model GroupProject.Pages.Subscriptions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Subscription[0].Date)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Subscription[0].Client)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subscription[0].Package)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Subscription) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Client.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Package.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SubscriptionID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SubscriptionID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SubscriptionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
